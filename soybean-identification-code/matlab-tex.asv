fOutTex = fopen([directoryResults tClassifier '_' featureType '.tex'],'w');

matrix = rand(5,5);
symbols = 'A','B','C','D','E');
for(=1:size(symbols))
    fprint(fOutTex,'%c ',symbols(i)
end
fprintf(fOutTex,'\\\\ \n');
for(i=1:size(matrix,1))
    for(j=1:size(matrix,2))
        fprintf(fOutTex,'%5.1f',matrix(i,j));
        if (j ~= size(matrix,2))
            fprintf(fOutTex,' & ',matrix(i,j));
        end
    end
    fprintf(fOutTex,'\\\\ \n');
end

fclose(fOutTex);
